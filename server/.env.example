# SecureChat 服务器环境配置
# 改完本文件后需重启服务器才生效。
# 说明：163 邮箱的 SMTP_PASS 必须是「客户端授权码」（开启 SMTP 服务后在邮箱设置里生成），
#       不是登录密码。格式为授权码则能发信成功；若用登录密码，163 会返回 535 认证失败。
SMTP_HOST=smtp.163.com
SMTP_PORT=465
SMTP_USER=andy130305@163.com
SMTP_PASS=JZmmd32V9UusCa3z
# SMTP__SEND_INTERVAL: (可选)发送间隔控制
