# SecureChat 服务器更新方案

## 一键更新

在项目根目录双击：

```text
一键上传SecureChat更新.bat
```

脚本会自动打包：

- `web/`
- `server/`
- `portable/`
- `data/version.json`

不会上传或覆盖：

- `data/chat.sqlite`
- Cloudflare Tunnel 凭据
- 服务器上的历史更新包和备份

然后向以下接口上传：

```text
POST https://mc.32768.top:5432/api/admin/update-package?apply=true
```

服务器会自动：

1. 校验 ZIP 路径，防止 Zip Slip
2. 解压到临时目录
3. 备份当前 `web/server/portable/version.json`
4. 替换新文件
5. 保留聊天数据库
6. 释放 8080 端口
7. 自动重启 Node 服务

## Token

脚本运行时会要求输入管理员 Bearer Token。Token 不写入脚本、不写入 ZIP。

## 回滚

备份目录位于服务器：

```text
server/update-packages/backups/
```

更新失败不会自动删除备份。服务器接口：

```text
GET /api/admin/update-status
```

可查看状态。

## 说明

服务器 EXE 不是更新功能的前置条件。当前方案直接更新正在运行的 Node 服务器，可靠性比强行等待 Node 单文件打包更高。
