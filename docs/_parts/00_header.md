# SecureChat 功能矩阵调研：微信 + QQ 全功能清单（5端对齐底稿）

> 面向 5 端（Web / Windows / macOS / iOS / Android）IM 应用做功能对齐的功能清单底稿。
> 端覆盖取值：`全端`（5端均有）`主端`（移动主端）`移`（iOS+Android）`桌`（Win+macOS+Web）`Win` `Web` `iOS` `安卓` `任意端`。不确定写 `全端/主端`。
> 现状列：`X`=现状已做，`待`=待做，`N/A`=不适用/暂停。优先级：P0=必做核心，P1=重要，P2=增强/长尾。
> 编号规则：`大类-连号`（01-001 …）。计数由脚本按表格行统计。

## 统计摘要（由脚本填充）
（脚本统计）

---